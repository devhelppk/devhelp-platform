# S5 review record

`/code-review high` ran on the S5 diff (verification pass completed; two claims refuted by it, ten confirmed findings reported, six more confirmed but cut by its cap). Every confirmed finding was fixed before landing.

| #   | Finding                                                                                                                                                                                                                                    | Fix                                                                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `resolveFlag` had no track scoping and, on "upheld", logged a `hide` action without hiding the item, in two separate statements: any mentor could resolve any flag and the audit log recorded a hide that never happened.                  | One transaction: the flag's item is locked through the same `scope()` as the queue (flags with no item are admin-only); upholding a flag on an approved item hides it and logs `hide` with before/after; otherwise `dismiss_flag`. Tests added.                                           |
| 2   | The mentor scope in `flags` reduced to "flags with no item", the inverse of the rule.                                                                                                                                                      | Mentors see flags whose item is in their tracks (subquery on `moderation_items.track`); admins see all. Test: a career mentor cannot see or resolve a technical-track flag.                                                                                                               |
| 3   | `sendOnSignUp` awaited the verification mail with no guard: an SMTP outage turned sign-up into a 500 with an orphaned, never-mailed user.                                                                                                  | The hook catches and logs on the sign-up path (the account page offers a resend) and still throws for an explicit resend so the user sees the error.                                                                                                                                      |
| 4   | `/verify-email?next=//evil.com` passed the `startsWith("/")` check and became a link and a sign-in callback: an open redirect on the real domain. The notifications "open" action had the same check.                                      | `safePath()` in `apps/lms/lib` rejects absolute, protocol-relative, and backslash forms; `safeCallback`, the verify page, and the notifications action use it.                                                                                                                            |
| 5   | Verification links never landed on `/verify-email`: the resend used `/account?verified=1` (which then showed "Email verified" on an expired token) and sign-up links used the form's callback, so the expired-link branch was unreachable. | Both flows use `/verify-email?next=…`, which branches on `emailVerified` and `error`; the account banner also checks `!error`.                                                                                                                                                            |
| 6   | `rateLimit()` counted before the guarded work in its own statement, so a transient failure of the work still consumed the allowance (a 30-day lockout for mentor applications with nothing on file).                                       | `rateLimit` takes a transaction and is called inside the transaction of the work it guards (application, resend, flag, review request); a failed attempt rolls the counter back.                                                                                                          |
| 7   | `checkPairs` refused `EMAIL_PROVIDER=log` whenever `NODE_ENV=production`, which `next build` and `next typegen` set, so any pre-S5 `.env` broke `check-types` and `build`.                                                                 | The production guard moved to runtime (`getTransport()`, on the first email); env validation no longer needs mail credentials to build.                                                                                                                                                   |
| 8   | Role and verification changes were invisible to an open session for up to 5 minutes (cookie cache), so the "you are now a mentor" link 404'd. Better Auth's `cookieCache.version` cannot help: it only sees the cached copy of the user.   | `mentorProcedure` and `verifiedProcedure` read role and `emailVerified` from the users table (one primary-key query); the `/moderate`, `/mentor/apply`, and `/verify-email` pages call `getSession` with `disableCookieCache`. The loop's admin promotion now works without a re-sign-in. |
| 9   | `requestReview` and `flag` wrote the item/flag and its audit row in two statements, and `requestReview` had no rate limit.                                                                                                                 | Both run in one transaction with a rate limit (5 review requests and 20 flags per day).                                                                                                                                                                                                   |
| 10  | `updateProfile` mapped an empty city to `undefined`, so a saved city could never be cleared.                                                                                                                                               | An empty string is accepted and passed through.                                                                                                                                                                                                                                           |
| 11  | Hiding the Courses header link below `sm` removed it for signed-out visitors (the account menu only shows "Sign in").                                                                                                                      | The theme toggle is hidden below `sm` instead (the theme follows the system there); Courses stays.                                                                                                                                                                                        |
| 12  | `?status=constructor` passed the `in statusLabels` guard on `/moderate`.                                                                                                                                                                   | `Object.hasOwn`.                                                                                                                                                                                                                                                                          |
| 13  | `decide` called `notify()` after the commit with no guard, so a blip there returned 500 for an already-decided item.                                                                                                                       | Wrapped and logged; the decision is the record.                                                                                                                                                                                                                                           |
| 14  | Templates import the `react-email` umbrella, which every `getSession` paid for through `@repo/auth`.                                                                                                                                       | `@repo/auth` and the moderation router load `@repo/email` with a dynamic import at the moment an email is sent.                                                                                                                                                                           |
| 15  | The queue and inbox indexes could not serve the `ORDER BY created_at DESC` queries.                                                                                                                                                        | Migration `0007_queue_indexes`: `(status, created_at)` for the queue, `(user_id, created_at)` for the inbox plus `(user_id, read_at)` for the unread count.                                                                                                                               |
| 16  | Leftovers in `trpc.ts`: `void and`, an unused `adminProcedure`, an unread `ctx.role`.                                                                                                                                                      | Removed.                                                                                                                                                                                                                                                                                  |

Refuted by the review's verification pass (no change): CI's `EMAIL_PROVIDER=smtp` does not override vitest's `test.env`; the hand-written `Invitation` type in `accept-invitation.tsx` is needed because the client's `getInvitation` resolves to `any`.

Deliberate leftovers

- Resend has not been exercised against the real API (no account yet; founder action). The transport is 30 lines over the official SDK.
- The email digest (X4) is not built; each kind declares whether it emails immediately, so the digest is a scheduler change later.
- `adminProcedure` was removed as unused; mentor applications are gated inside `decide` by role instead. Add it back when an admin-only router appears (S10/S11).
